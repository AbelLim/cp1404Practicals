# It seems like you cannot use the first BoxLayout class as your entity container.
# Create a second BoxLayout for the container.
BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'vertical'
        id: entity_container